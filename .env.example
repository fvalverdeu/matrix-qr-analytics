# Host ports exposed by Docker Compose
QR_SERVICE_PORT=8080
STATISTICS_SERVICE_PORT=3000

# QR Service configuration (used when running locally outside Docker)
STATISTICS_SERVICE_URL=http://localhost:3000
STATISTICS_TIMEOUT_MS=5000

# Inside Docker Compose, STATISTICS_SERVICE_URL is set to:
# http://statistics-service-node:3000
